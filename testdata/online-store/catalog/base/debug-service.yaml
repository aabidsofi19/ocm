apiVersion: v1
kind: Service
metadata:
  name: catalog-debug
  labels:
    app: catalog
    purpose: debug
spec:
  type: NodePort
  selector:
    app: catalog
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500
